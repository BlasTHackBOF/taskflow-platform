# Everything TaskFlow-owned lives in its own namespace: RBAC and quotas
# scope to it, `kubectl delete namespace taskflow` is a clean uninstall,
# and the monitoring stack (next phase) stays separate. See ADR-0009.
apiVersion: v1
kind: Namespace
metadata:
  name: taskflow
