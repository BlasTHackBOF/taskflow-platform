# Ansible-managed SSH hardening. sshd takes the FIRST value it sees per
# option, and drop-ins are read in lexical order before the main config —
# the 00- prefix makes these values win over everything else.
PermitRootLogin no
PasswordAuthentication no
KbdInteractiveAuthentication no
X11Forwarding no
MaxAuthTries 4
